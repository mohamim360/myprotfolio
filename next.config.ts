import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com',"img.shields.io"], // Allow images from this domain
  },
};

export default nextConfig;
